variables:
  - name: System.Debug
    value: true

pool:
  vmImage: 'macOS-latest'

steps:  
  - task: CmdLine@2
    displayName: 'Install Core Dependencies'
    inputs:
      script: |
        yarn install
